{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Run list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Step</th>
                <th>Time1</th>
                <th>Time2</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('run_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.step }}</td>
                <td>{{ entity.time1 }}</td>
                <td>{{ entity.time2 }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('run_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('run_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('run_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
